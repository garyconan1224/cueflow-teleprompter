# 智能提词器 —— 第一次下载先读我

欢迎！这份文档帮你从零开始把智能提词器跑起来。

---

## 这是什么？

**智能提词器** 是一个本地运行的中文语音提词器。你对着麦克风朗读，浏览器里的提词内容会自动跟着你的声音向上滚动。

> 不需要联网，不需要 GPU，普通 Windows 笔记本就能跑。

---

## 第一步：安装 Python

项目需要 **Python 3.10 或更高版本**。

1. 打开 [python.org](https://www.python.org/downloads/)
2. 下载最新版 Python，**安装时务必勾选 "Add Python to PATH"**
3. 安装完成后，打开 PowerShell 或 CMD，验证：

```powershell
python --version
```

应该显示类似 `Python 3.12.x` 的信息。

---

## 第二步：解压项目

如果你下载的是便携包（`cueflow-teleprompter-portable.zip`）：

1. 右键 → 全部解压
2. 解压到任意目录，比如 `D:\智能提词器\`
3. **目录路径不要包含中文或空格**，避免兼容性问题

如果你是从 GitHub 下载的源码：

```powershell
git clone https://github.com/garyconan1224/voice-teleprompter.git
```

---

## 第三步：一键配置环境

进入项目目录，双击：

```
setup_portable_env.bat
```

这个脚本会自动完成：

- 创建 Python 虚拟环境（`.venv`）
- 安装所有依赖（FastAPI、FunASR、RapidFuzz 等）
- 下载语音识别模型（约 200MB，需联网，仅首次执行）
- 构建前端页面

> 整个过程大约 5-10 分钟，取决于网速。请耐心等待。

如果脚本中途报错，检查：
- Python 是否已正确安装并添加到 PATH
- 网络是否通畅（模型下载需要访问 ModelScope）
- 目录路径是否包含中文或空格

---

## 第四步：启动程序

配置完成后，每次使用只需双击：

```
run_portable_app.bat
```

看到类似输出说明启动成功：

```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

---

## 第五步：打开浏览器

在浏览器地址栏输入：

```
http://127.0.0.1:8000
```

你会看到智能提词器的主界面。

---

## 快速上手

### 界面布局

```
┌─────────────────────────────────────┐
│☰  ← 切换侧栏                        │
│   ┌────────┐  ┌──────────────────┐  │
│   │ 脚本编辑 │  │                  │  │
│   │         │  │   提词器镜面     │  │
│   │ 连接面板 │  │   （始终居中）    │  │
│   │ 控制面板 │  │                  │  │
│   └────────┘  └──────────────────┘  │
└─────────────────────────────────────┘
```

### 开始提词

1. **输入脚本** — 在左侧"脚本编辑"中粘贴或导入你的演讲稿
2. **连接后端** — 点击"连接后端"按钮（默认已连好本地服务）
3. **开始识别** — 点击"开始麦克风识别"
4. **开始朗读** — 对着麦克风朗读，提词内容自动跟随滚动

### 实用技巧

| 操作 | 方法 |
|------|------|
| 收起侧栏 | 点击左上角 ☰ 按钮，获得全屏提词视野 |
| 折叠面板 | 点击面板标题栏（脚本编辑/后端识别链路/提词器控制台） |
| 微调位置 | 鼠标滚轮滚动提词器区域 |
| 快速跳转 | 按住 Shift + 滚轮，大步跳跃 |
| 全屏提词 | 点击"提词器全屏"按钮 |
| 副屏显示 | 控制台选择"双屏"模式，在另一台显示器打开提词窗口 |

---

## 外接麦克风使用

如果想用外接麦克风（推荐），需要在系统设置中把外接麦克风设为默认输入设备：

1. Windows 设置 → 系统 → 声音 → 输入
2. 选择你的外接麦克风
3. 确保音量条有反应

---

## 常见问题

### 1. "连接后端失败"

确认 `run_portable_app.bat` 窗口还在运行，没有关闭。

### 2. "启动麦克风失败"

检查浏览器是否允许网页访问麦克风。Chrome 地址栏左侧会有权限提示。

### 3. "识别不准确"

- 确保环境安静
- 麦克风不要离嘴太远
- 语速适中，吐字清晰

### 4. "更新代码后页面没变化"

双击 `build_frontend.bat` 重新构建前端，然后刷新浏览器。

---

## 开发相关

如果你是开发者，想修改代码：

```powershell
# 启动后端（开发模式）
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --reload

# 启动前端开发服务器（另一个终端）
cd frontend
npm install
npm run dev
# 访问 http://localhost:5173
```

### 运行测试

```powershell
# 后端测试
python -m unittest tests.test_matcher tests.test_websocket

# 前端测试
cd frontend
npx playwright install chromium
npm run test:e2e
```

---

## 目录结构

| 路径 | 说明 |
|------|------|
| `backend/` | FastAPI 后端 + FunASR 语音识别 |
| `frontend/` | React + TypeScript 前端 |
| `tests/` | 后端单元测试 |
| `screenshots/` | 界面截图 |
| `run_portable_app.bat` | 一键启动脚本 |
| `setup_portable_env.bat` | 环境配置脚本 |
| `build_frontend.bat` | 前端构建脚本 |

---

有问题或建议？欢迎在 [GitHub Issues](https://github.com/garyconan1224/voice-teleprompter/issues) 反馈。
